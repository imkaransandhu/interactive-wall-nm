import styles from "./CaptureSession.module.css";

const CaptureSession = () => {
  return (
    <div className={styles["capture-session"]}>
      <p>Capture Session: 45s Remaining</p>
    </div>
  );
};

export default CaptureSession;
